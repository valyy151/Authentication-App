<div class='indexDiv'>
	<h1>{{title}}</h1>
	{{#if username}} <h2>Hello, {{username}}</h2>{{/if}}
	
	
	{{#unless username}}<h3>Cool Goose photos below!</h2>{{/unless}}
    {{#if username}} <h3>You can now see the cool Goose photos!</h3> {{/if}}

	
	<form action='/photos' method='GET'>
		<button>Show me!</button>
	</form></div>